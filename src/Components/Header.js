import React from 'react';


const Header = ({title}) => {

    // const { title } = props;
    
    return (
        <header>
          <h1>{title}</h1>
        </header>
    );
};

export default Header;